# React Form for Submitting Requests for Assistance

To install dependencies run `npm install`

Then run `npm run start` to start a development server at localhost:8080

`npm run test` to run jest tests
